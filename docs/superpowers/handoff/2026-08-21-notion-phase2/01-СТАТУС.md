# Статус на 2026-08-21

## Сделано

### Фаза 1 — модерация учителей: ЗАКРЫТА ПОЛНОСТЬЮ, всё в проде

Все три компонента выложены и проверены:

| Компонент | Состояние |
|---|---|
| tucan-bot | ✅ `status draft/pending/approved`, эндпоинты submit-for-review и `/:id/status`, 4 учителя backfill → approved |
| landing | ✅ фильтрует `status = 'approved'`, отдаёт реальных учителей из `tukan` |
| tucan (админка) | ✅ выложена на VPS 2026-07-28, коммит `58fcb19`, `Compiled successfully`, 152.15 kB gz |

Автоперевод анкет (`POST /teacher-anketa/:id/translate`) вмёржен, но **спит**: кнопка из
админки убрана, без `ANTHROPIC_API_KEY` недостижим. Включение отложено в Фазу 5.

### Фаза 2 — план написан, исполнение НЕ начиналось

- ✅ План на 12 задач написан и самопроверен против спеки (покрытие требований, отсутствие заглушек, консистентность типов между задачами).
- ✅ Решение владельца по модалке принято и внесено в план (см. ниже).
- ✅ Разведка окружения проведена — результаты в [02-ФАКТЫ-И-ГРАБЛИ.md](02-ФАКТЫ-И-ГРАБЛИ.md).
- ✅ Выбран способ исполнения: **subagent-driven** (свежий субагент на задачу, ревью между задачами).
- ❌ **Ни одна задача не выполнена. Коммитов по Фазе 2 нет ни в одном репозитории.**

Успели только настроить окружение исполнения:

- SDD-workspace: `TucanBRAS/.superpowers/sdd/2026-07-28-notion-removal-phase2-cms/` — создан, **пустой**.
- Леджера `progress.md` внутри нет → новый чат стартует с чистого листа, задача 1.
- Worktree'ы для изоляции **не созданы** — это первое, что нужно сделать.

## Решение владельца, внесённое в план (2026-08-21)

**`FreeLessonModal` удаляется совсем.** Компонент не монтировался с момента редизайна
(403 строки мёртвого кода, на него никто не ссылается кроме комментария в `TutorAvatar.tsx`).

Последствия, уже разложенные по задачам плана:

| | Было | Стало |
|---|---|---|
| Секций в CMS | 9 | **8** |
| Строк после сидинга | 27 | **24** (8 × 3 локали) |
| Форм в админке | 8 из 9 | **8 из 8** |

**Важно для Фазы 3:** спека требует добавить «Хочу стать преподавателем» *«в селекте учителя
обеих форм (`FreeLessonModal`, `FooterForm`)»* — форм теперь одна, футерная. Спеку в этом
месте нужно поправить, когда дойдёте до Фазы 3.

## Что изменилось прямо перед передачей — ВАЖНО

**Предусловие части C выполнено, план в этом месте устарел.**

План говорит: «часть C (лендинг) заблокирована, сначала закоммить редизайн — в дереве
779+/545− незакоммиченных строк». На момент написания плана это было правдой. **Сейчас
редизайн закоммичен**, проверено:

```
TucanBRAS: ветка dev, HEAD = 94256e8, git status --porcelain пуст
landing/lib/uiLabels.ts          → отслеживается, коммит 8ce6f59
landing/components/sections/Header.tsx → коммит 6967913
```

Содержимое совпадает с тем, на чём построен план, — проверено точечно:
- `page.tsx` содержит `import { uiLabels }` (строка 21) и логику `notionFailed` (строки 73–87) — Task 9 их и удаляет;
- снапшот содержит ребрендинг во всех трёх локалях (`Туторы` / `Tutors` / `Tutores`) и 9 секций, включая `modal`.

**Значит части A, B и C можно исполнять подряд, без ожидания.** Предусловие в шапке части C
(«Начинать Часть C можно только после того, как редизайн закоммичен») уже удовлетворено —
достаточно ещё раз убедиться, что `git status --porcelain` пуст, и идти дальше.

> Оговорка: в середине сессии дерево TucanBRAS показывало 21 изменённый файл, а через
> несколько минут стало чистым при неподвижном HEAD. Вероятнее всего, параллельная сессия
> Claude, работающая в этом же worktree, довела коммиты (память проекта прямо предупреждает:
> «чужой коммит ложится поверх твоего за секунды»). Данные не потеряны — все файлы на месте
> и отслеживаются. Но **перед любым amend/reset сверяйте HEAD со своим коммитом.**

## Следующее действие

1. Создать worktree'ы для изоляции (см. [02-ФАКТЫ-И-ГРАБЛИ.md](02-ФАКТЫ-И-ГРАБЛИ.md), раздел «Изоляция»).
2. Завести леджер `progress.md` в SDD-workspace.
3. Начать с **Task 1** плана (модель `LandingContent` + вайтлист секций, репозиторий tucan-bot).

Порядок задач жёсткий и обусловлен зависимостями: **A (бот, 1–4) → B (админка, 5–6) →
C (лендинг, 7–11) → 12 (деплой и приёмка)**. Админка бесполезна без API бота; лендинг
не сможет читать пустую таблицу, пока не прошёл сидинг.
