# Статус на 2026-08-21

> **Редакция 2.** Первая редакция этого файла утверждала, что Фаза 2 не начата. Это было
> неверно: проверялись только локальные рабочие деревья, без удалённых веток и
> открытых PR. Ниже — перепроверенное состояние.

## Фаза 1 — модерация учителей: ЗАКРЫТА ПОЛНОСТЬЮ

Все три компонента в проде:

| Компонент | Состояние |
|---|---|
| tucan-bot | ✅ `status draft/pending/approved`, эндпоинты submit-for-review и `/:id/status`, 4 учителя backfill → approved |
| landing | ✅ фильтрует `status = 'approved'`, отдаёт реальных учителей из `tukan` |
| tucan (админка) | ✅ выложена на VPS, коммит `58fcb19`, `Compiled successfully`, 152.15 kB gz |

Проверено 2026-08-21: ветка `feat/teacher-moderation` содержит **0 уникальных коммитов**
относительно `origin/main` — влита целиком. Открытых PR по Фазе 1 нет ни в одном
из трёх репозиториев. Локальный worktree `wt-tucanbras-moderation` — отработавший
остаток, можно снимать.

Автоперевод анкет (`POST /teacher-anketa/:id/translate`) вмёржен, но **спит**: кнопка
из админки убрана, без `ANTHROPIC_API_KEY` недостижим. Отложен в Фазу 5.

## Фаза 2 — лендинг-часть УЖЕ СДЕЛАНА, бот и админка нет

| Часть | Задачи плана | Репозиторий | Состояние |
|---|---|---|---|
| A — модель, API, сидинг | 1–4 | tucan-bot | ❌ **не начата**, веток нет |
| B — раздел «Лендинг» в админке | 5–6 | tucan | ❌ **не начата**, веток нет |
| C — чтение контента из БД | 7–11 | landing | ✅ **сделана**, PR #15 (черновик) |
| Деплой и приёмка | 12 | все | ❌ не начат |

### PR #15 — что в нём

`cherenkoov/tucanbras` **#15** «Фаза 2: лендинг читает контент из Postgres»,
ветка `claude/nice-bell-rvelin`, черновик, создан 2026-08-21 03:07 параллельной
сессией по этому же плану. 5 коммитов с названиями ровно из задач 7–11.

Заявленное в описании **проверено по содержимому ветки**:

- `lib/notion.ts` — удалён ✓
- `lib/content.ts` — существует, ровно **8** экспортированных геттеров секций ✓
- `lib/contentSnapshot.json` — **8 секций × 3 локали**, секции `modal` нет ✓
- `NotionRetry.tsx`, `FreeLessonModal.tsx`, `scripts/snapshot-notion.ts` — удалены ✓
- `POST /api/revalidate`, `lib/contentResolve.ts` с тестами — на месте ✓

Автор заявляет `npx tsc --noEmit` чисто и `npm test` 12/12 — на слово, локально не перепроверялось.

**Состояние ветки:** отстаёт от `origin/main` на 2 коммита. Пересечения изменённых
файлов с этими двумя коммитами **нет** → конфликтов при мерже не ожидается, но
обновить ветку перед мержем нужно. GitHub показывает `mergeable: UNKNOWN` — обычное
дело для черновика, пересчитается при снятии draft.

## Порядок работ переворачивается — читать внимательно

Лендинг уже готов читать таблицу `LandingContents`, **которой ещё не существует**.
Мержить PR #15 первым бессмысленно: без таблицы лендинг просто останется на
снапшот-фолбэке (сломаться не сломается — фолбэк ровно для этого и сделан).

Правильная последовательность:

1. **Бот, задачи 1–4** — модель `LandingContent`, `GET/PUT /api/landing-content`,
   revalidate-хук, сидинг 24 строк. Отсюда и начинать.
2. **Админка, задачи 5–6** — схема секций и раздел «Контент лендинга».
3. **Обновить ветку PR #15** от `origin/main`, снять draft, смержить.
4. **Задача 12** — деплой в порядке бот → сидинг → админка → лендинг и приёмка
   по четырём критериям спеки.

## Решение владельца: FreeLessonModal удаляется совсем

Компонент не монтировался с момента редизайна (403 строки мёртвого кода).

| | Было в спеке | Стало |
|---|---|---|
| Секций в CMS | 9 | **8** |
| Строк после сидинга | 27 | **24** (8 × 3 локали) |
| Форм в админке | 8 из 9 | **8 из 8** |

В PR #15 это уже реализовано. **Для Фазы 3:** спека требует добавить «Хочу стать
преподавателем» *«в селекте учителя обеих форм (`FreeLessonModal`, `FooterForm`)»* —
форма теперь одна, футерная. Спеку в этом месте нужно поправить.

## Как не повторить мои ошибки

В сессии, собравшей эту папку, я дважды ошибся в оценке состояния, и оба раза
по одной причине — **смотрел только локальное состояние**:

1. Локальная ветка `main` отставала на 17 коммитов, и расхождение `dev`/`main`
   я посчитал против неё. Вывод «17 коммитов и 5238 строк ждут релиза» оказался
   ложным: редизайн уже был влит в `origin/main` через PR #13.
2. Не посмотрел удалённые ветки и открытые PR — и объявил Фазу 2 не начатой,
   хотя PR #15 с готовой лендинг-частью уже висел.

**Правило перед любой оценкой состояния:** `git fetch --all` во всех трёх
репозиториях плюс `gh pr list --state open` по каждому. В этих репозиториях
параллельно работают другие сессии Claude (в том числе облачные, их ветки
называются `claude/*`), и локальный чекаут отстаёт от реальности за минуты.

## Следующее действие

Начать с **задачи 1** плана (модель `LandingContent` + вайтлист секций) в репозитории
`tucan-bot`, ветка от `origin/main`. Команды создания worktree — в
[02-ФАКТЫ-И-ГРАБЛИ.md](02-ФАКТЫ-И-ГРАБЛИ.md).
