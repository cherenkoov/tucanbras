# Промпт для нового чата

Скопируй текст между линиями первым сообщением в новый чат.

---

Продолжаем Фазу 2 отказа от Notion — перенос текстов лендинга в CMS админки.

Передача собрана здесь, прочитай всю папку целиком перед стартом:
`c:\active-projects\TucanBRAS\docs\superpowers\handoff\2026-08-21-notion-phase2\`

Оттуда узнаешь состояние, факты окружения и грабли. План и спека лежат отдельно
(пути в README папки) — работай с ними как с каноническими.

**Состояние:** лендинг-часть плана (задачи 7–11) уже сделана параллельной сессией
и лежит в черновом PR #15 репозитория `cherenkoov/tucanbras`. **Бот (задачи 1–4)
и админка (задачи 5–6) не начаты — веток нет.** Начинать с бота: лендинг уже
готов читать таблицу `LandingContents`, которой ещё не существует.

Порядок: бот (1–4) → админка (5–6) → обновить ветку PR #15 от main и смержить →
деплой и приёмка (задача 12).

Три вещи, которые нельзя упустить:

1. **Перед оценкой состояния — `git fetch --all` во всех трёх репозиториях и
   `gh pr list --state open` по каждому.** В этих репозиториях параллельно работают
   другие сессии Claude (облачные ветки называются `claude/*`), локальный чекаут
   отстаёт за минуты. Предыдущая сессия дважды ошиблась именно на этом.
2. **Локальный `tucan-bot` стоит на протухшей ветке** (`fix/anketa-upload-corruption`,
   −19 от `origin/main`, без колонки `status`). Все ветки создавать от `origin/main`.
3. **В `c:\active-projects\tucan` лежат незакоммиченные файлы чужого трека**
   (редизайн онбординга Costa, открытый PR `Raison231/tucan#16`). Не трогать —
   работать через worktree.

Исполняем через skill `superpowers:subagent-driven-development` — свежий субагент на
задачу, ревью между задачами. SDD-workspace:
`TucanBRAS/.superpowers/sdd/2026-07-28-notion-removal-phase2-cms/`, леджера внутри нет.

Начни с создания worktree для бота (команды в `02-ФАКТЫ-И-ГРАБЛИ.md`), заведи леджер
и отправляй первого субагента на задачу 1.

---

## Если хочешь исполнять вручную, без субагентов

Замени последний абзац на:

> Исполняем вручную, задача за задачей. Читай план, делай задачу 1, показывай мне диф,
> потом задачу 2 и так далее. После каждой задачи прогоняй тесты того репозитория,
> в котором работал.
