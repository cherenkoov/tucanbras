# Промпт для нового чата

Скопируй текст между линиями первым сообщением в новый чат.

---

Продолжаем Фазу 2 отказа от Notion — перенос текстов лендинга в CMS админки.

Передача собрана здесь, прочитай всю папку целиком перед стартом:
`c:\active-projects\TucanBRAS\docs\superpowers\handoff\2026-08-21-notion-phase2\`

Оттуда узнаешь состояние, факты окружения и грабли. План и спека лежат отдельно
(пути в README папки) — работай с ними как с каноническими.

Коротко: план на 12 задач написан, **ни одна не выполнена**, коммитов по Фазе 2 нет.
Исполняем через skill `superpowers:subagent-driven-development` — свежий субагент на
задачу, ревью между задачами. SDD-workspace уже посчитан:
`TucanBRAS/.superpowers/sdd/2026-07-28-notion-removal-phase2-cms/`, леджера внутри нет,
стартуем с задачи 1.

Три вещи, которые нельзя упустить:

1. **Локальный `tucan-bot` стоит на протухшей ветке** (`fix/anketa-upload-corruption`,
   −19 от `origin/main`, без колонки `status`). Все ветки создавать от `origin/main`.
2. **В `c:\active-projects\tucan` лежит 25 незакоммиченных файлов чужого трека**
   (редизайн онбординга). Не трогать — работать через worktree.
3. **Порядок задач жёсткий:** бот (1–4) → админка (5–6) → лендинг (7–11) → деплой (12).

Предусловие части C в плане («сначала закоммить редизайн лендинга») **уже выполнено** —
дерево TucanBRAS чистое на `dev @ 94256e8`. Просто перепроверь `git status --porcelain`
и иди дальше, ждать нечего.

Начни с создания worktree'ов (команды в `02-ФАКТЫ-И-ГРАБЛИ.md`), заведи леджер и
отправляй первого субагента на задачу 1.

---

## Если хочешь исполнять вручную, без субагентов

Замени последние два абзаца на:

> Исполняем вручную, задача за задачей. Читай план, делай Task 1, показывай мне диф,
> потом Task 2 и так далее. После каждой задачи прогоняй тесты того репозитория,
> в котором работал.
